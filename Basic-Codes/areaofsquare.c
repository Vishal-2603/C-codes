/*prog to calculate area of square*/
#include<stdio.h>
int main()
{
   int side;
   int area;
 printf("enter side of square");
 scanf("%d",&side);
  area=side*side;
  printf("area of square is %d",area);
}
